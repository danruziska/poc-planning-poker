{
  "name": "poc-planning-poker",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "create-react-app-sass": "^1.1.0",
    "node-sass": "^4.5.0",
    "react-scripts": "0.9.0",
    "sass-loader": "^6.0.0"
  },
  "dependencies": {
    "react": "^15.4.2",
    "react-dom": "^15.4.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "npm run build-css && react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "build-css": "node-sass src/stylesheets/main.scss src/index.css",
    "watch-css": "nodemon -e scss -x \"npm run build-css\""
  }
}
