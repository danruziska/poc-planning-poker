import { blue, white, yellow, black } from './base-colors';

export const primaryColors = {
     primaryBackground: blue,
     primaryTextColor: white
}

export const secondaryColors = {
    secondaryBackground: yellow,
    secondaryTextColor: black
}