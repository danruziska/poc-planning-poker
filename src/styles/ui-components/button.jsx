import * as colors from '../base/base-colors';

export const defaultButton = {
    backgroundColor: colors.red500,
    color: colors.white
}

export const alternateButton = {
    backgroundColor: colors.yellow500,
    color: colors.black
}